# Database
DATABASE_URL=postgresql://see_user:see_password@db:5432/see_db

# NextAuth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=          # Generate: openssl rand -base64 32

# SMTP (Gmail)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=tbsfnoreply@gmail.com
SMTP_PASS=                # Gmail App Password
SMTP_FROM=IT Security <tbsfnoreply@gmail.com>

# App
BASE_URL=http://localhost:3000    # Public URL for tracking links

# Initial Admin (used by seed script)
ADMIN_EMAIL=admin@see.local
ADMIN_PASSWORD=           # Will be bcrypt hashed by seed script
